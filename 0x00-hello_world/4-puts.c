#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 * puts prints string to stdio with a trailing new line
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle");

	return (0);
}
