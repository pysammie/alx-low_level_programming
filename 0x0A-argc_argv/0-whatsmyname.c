#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: argument counter
 * @argv: array of arguments
 *
 * Return: 0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
